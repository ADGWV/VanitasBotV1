# 🤖 VanitasBot-MD

بوت واتساب Multi-Device مبني بعقل، مش بتجميع.
هيكل نظيف، ملفات واضحة، وتوسعة بدون ما المشروع ينهار.

> **VanitasBot-MD** مش نسخة من بوت تاني  
> ده مشروع معماري معمول عشان يعيش ويكبر.

---

## 🧠 الفكرة

معظم بوتات واتساب:
- ملفات متداخلة
- لوجيك ملخبط
- أي تعديل = كسر حاجة تانية

VanitasBot-MD اتبنى بعكس كده:

- فصل مسؤوليات واضح
- كل ملف له دور واحد
- أي إضافة جديدة = Plugin مستقل
- لا كود مسروق
- لا اعتماد على “سحر أسود”

---

## 🗂️ هيكل الملفات
VanitasBot-MD/
│
├── index.js            # نقطة الدخول – تشغيل ومراقبة
├── main.js             # تنسيق وتشغيل كل الأجزاء
├── socket.js           # اتصال WhatsApp (Baileys)
├── auth.js             # QR / Pairing Code / Sessions
├── events.js           # كل أحداث واتساب
├── handler.js          # فهم الرسائل والأوامر
├── reply.js            # بناء الردود (نص / زر / قائمة)
├── media.js            # وسائط (ملصقات – صوت – تحميل)
├── database.js         # تخزين البيانات
├── permissions.js      # صلاحيات المستخدمين
├── features.js         # تشغيل / إيقاف الخصائص
├── plugins.js          # تحميل الإضافات
├── terminal.js         # شكل التيرمكس واللوجات
├── config.js           # إعدادات عامة
├── constants.js        # ثوابت ورسائل داخلية
│
├── plugins/            # أوامر البوت (Modules)
├── i18n/               # اللغات
├── sessions/           # جلسات واتساب
├── media/              # ملفات الميديا
├── logs/               # السجلات
├── tmp/                # مؤقت
│
├── official_accounts.json
├── app.json
├── .env
├── .env.example
├── .gitignore
├── package.json
└── README.md

---

## ⚙️ المتطلبات

- Node.js **v18 أو أحدث**
- npm أو yarn
- حساب واتساب

---

🚀 VanitasBot-MD — Installation (Termux)

> بوت واتساب MD مبني بعقل، مش بعك.
الخطوات دي مجرّبة، نظيفة، وتشتغل من أول مرة.

---

### 1️⃣ إعداد Termux (مرة واحدة بس)

```bash
termux-setup-storage
```
```bash
apt update -y && apt upgrade -y
```

---

### 2️⃣ تثبيت المتطلبات الأساسية

```bash
pkg install -y
```
```bash
git 
```
```bash
nodejs 
```
```bash
ffmpeg 
```
```bash
imagemagick 
```
```bash
python 
```
```bash
build-essential 
```
```bash
libwebp 
```
```bash
openssl
```

## `ليه دول؟ باختصار:`

- `nodejs` → `قلب البوت`
- `ffmpeg / imagemagick / libwebp` → `ميديا + ملصقات`
- `python + build-essential` → `native modules (من غيرهم npm هيعيط)`
- `openssl` → `مشاكل SSL خفية`

---

### 3️⃣ تحميل البوت

```bash
git clone https://github.com/USER/VanitasBot-MD
```
```bash
cd VanitasBot-MD
```

---

### 4️⃣ إعداد البيئة

```bash
cp .env.example .env
```

- عدّل .env وحط:

`رقم الـ owner`
`اسم البوت`
`أي مفاتيح API لاحقًا`

---

### 5️⃣ تثبيت الحزم

####❗ اختار طريقة واحدة فقط

- `باستخدام npm (الموصى به)`

```bash
npm install
```

- `أو باستخدام yarn`

```bash
yarn install
```

- ❌ ممنوع الاتنين مع بعض
- ❌ ممنوع لعب في lock files

---

### 6️⃣ تشغيل البوت

```bash
npm start
```

#### أثناء التطوير:

```bash
npm run dev
```

---

### 🧠 ملاحظات مهمة

#### أول تشغيل هيطلب:

- QR Code أو

- Pairing Code (8 أرقام)

- الجلسة تتحفظ تلقائيًا في sessions/

- لو البوت وقع → index.js يعيد تشغيله لوحده

---